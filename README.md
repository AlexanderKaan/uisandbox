# UIsandbox

Your existing app, 1:1, in a sandbox — then turn the knobs. Start with `PROMPT.md`.

- `PROMPT.md`  — the opening prompt for the first chat (the idea, the box, the hard parts, sprint 1)
- `HANDOFF.md` — what each file in `foundation/` is, where it came from, what it depends on
- `notes/`     — the lessons carried over from UIcockpit (`traps.md` first)
- `foundation/` — copied source from UIcockpit @ 1eecb19: tokens engine · panel · state · export · audit reader + intake · bench
